[package]
name = "huffi-protocol"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"
description = "Wire protocol for the huffi adaptive application launcher (JSON-over-Unix-socket)"
repository = "https://github.com/suiluj482/huffi"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
